{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.61.0"
  },
  "scripts": {
    "live-server": "live-server",
    "sass:dev": "sass --watch ./sass/header.scss ./css/style.css",
    "dev":"npm-run-all --parallel sass:dev live-server"
  }
}
